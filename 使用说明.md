# Nextop → 飞书多维表格 自动同步工具

自动抓取 Nextop CRM 工单 → DeepSeek 分析 → 写入飞书多维表格（ITR）。

---

## 环境要求

- Python 3.10 或以上
- Windows 系统

---

## 首次安装

**1. 安装依赖库**

```
pip install -r requirements.txt
playwright install chromium
```

**2. 配置文件**

`config.py` 已经预填好共用的飞书 App / 表格 / DeepSeek Key，不需要改动。

Nextop 的 token 不用手动填，通过下面的"更新 Token"步骤自动写入。

---

## 首次飞书授权（只需做一次）

运行：

```
python feishu_auth.py
```

浏览器会弹出飞书登录页，用**你自己的飞书账号**登录授权，完成后 token 自动写入 config.py。之后创建的记录都会显示你的名字。

---

## 更新 Nextop Token（每次 token 过期时）

Token 有效期约 1-2 天，过期后脚本会提示 `会话已过期`。

**步骤：**

1. 浏览器打开 [https://saas.nextop.com](https://saas.nextop.com) 并登录
2. 按 `F12` 打开开发者工具 → 切到 `Network` 标签
3. 随便点开一个工单
4. 在 Network 里找到任意一条 `pageOrders` 或 `basicInfo` 请求
5. 右键该请求 → **Copy → Copy as cURL (cmd)**
6. 运行 `python update_token.py`，按提示粘贴，输入 `END` 回车

完成后 config.py 会自动更新，不需要重启脚本。

---

## 日常使用

双击 `run.bat` 启动，或在命令行运行：

```
python main.py
```

**输入工单号**（如 `E199585`）→ 自动抓取、分析、写入飞书。

- 如果是**新工单**：自动创建新记录
- 如果是**已存在的工单**：自动更新所有字段（Solutions、Status、回复时间等）

**输入 `lark`** → 进入 Lark 消息模式，粘贴 Lark 沟通记录后输入 `END` 回车，自动分析写入（Reference No. 填 LARK，Total Replied 默认 2）。

**输入 `email`** → 进入邮件模式，粘贴邮件内容后输入 `END` 回车，自动分析写入（Reference No. 填 EMAIL，Total Replied 默认 2）。

**直接回车（空输入）**→ 退出程序。

---

## 字段填写规则

| 飞书字段 | 来源 |
|---------|------|
| Reference No. | 工单号（或邮件中的 CaseID） |
| PIE-Comment | AI 概括的问题摘要（中文） |
| Description | 邮件正文关键内容（保留英文） |
| Solutions | 已采取的解决方案 |
| Status | Open / Replied / Closed |
| Replied Time-First | Xavier 或 Reggie 首次回复时间 |
| Replied Time-NEW | Xavier 或 Reggie 最近回复时间 |
| Total Replied | Xavier + Reggie 的回复次数（上限 10） |

**账号开通特殊规则：**
- 仅当 PIE 回复中**实际给出了账号密码**时，才判定为"账号开通"
- 客户说"无法登录"、PIE 要求提供录屏 → 不是账号开通，按登录故障处理

---

## 常见问题

**Q: 提示"会话已过期"**
→ 按上方"更新 Nextop Token"步骤操作

**Q: 提示"DeepSeek 调用失败"**
→ 检查 config.py 中的 DEEPSEEK_API_KEY 是否正确；网络是否能访问 api.deepseek.com

**Q: 飞书写入失败（token 相关错误）**
→ 飞书 User Access Token 过期，脚本会自动用 Refresh Token 续期；如果持续失败，需要重新走飞书 OAuth 授权流程

**Q: 分析结果不准确**
→ 重新运行同一工单号，脚本会更新已有记录

---

## 文件说明

## V2 人工来源 Case 更新（以本节为准）

可输入 `whatsapp`、`lark` 或 `email`，粘贴本次新增沟通后以单独一行 `END` 结束。

- 首次沟通：系统会创建新的 ITR Case；Reference No. 分别为 `WHATSAPP`、`LARK`、`EMAIL`。
- 后续沟通：系统只读取同来源的少量候选 Case，并按设备、错误码、Dealer、机型、故障现象、时间和简短问题描述进行可解释排序。
- 系统不会自动合并 Case。它始终显示最多 5 个候选，由用户输入编号确认更新，或输入 `N` 创建新 Case。
- 选择已有 Case 后，系统会按飞书 `record_id` 重新读取最新 Case History，追加本次内容，并基于完整历史重新分析和更新 V2 受管字段。
- 如果本次粘贴内容已经完整存在于所选 Case History，系统会阻止重复追加。
- 无法可靠识别 PIE 发言人时，系统不会伪造 Status、回复时间或 Total Replied。

`Ticket No.` 用于候选展示；底层更新始终使用飞书 `record_id`。

| 文件 | 说明 |
|------|------|
| `main.py` | 主程序入口 |
| `analyzer.py` | DeepSeek 分析逻辑 |
| `nextop_api.py` | Nextop CRM API 封装 |
| `feishu_api.py` | 飞书 Bitable API 封装 |
| `update_token.py` | 手动更新 Nextop Token |
| `field_options.py` | 飞书单选字段选项列表 |
| `images.py` | 工单图片识别与去重逻辑 |
| `feishu_auth.py` | 首次飞书账号授权（获取你自己的 user token） |
| `config.py` | 配置文件（含密钥，勿转发给他人） |
| `run.bat` | Windows 一键启动脚本 |
