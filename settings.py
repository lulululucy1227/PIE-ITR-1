"""Non-sensitive operational settings for NextopSync."""

PIE_SENDERS = {
    "xavier",
    "reggie",
}

# Exact, non-sensitive Nextop person/account aliases.  The mapped value is
# still written only when it is an exact current Feishu Dealer option.
NEXTOP_DEALER_ALIASES = {
    "Gaetan Forton": "Greenlook",
}
